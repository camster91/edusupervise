{
  "name": "@edusupervise/sms",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SMS provider adapter — mock + Twilio implementations for EduSupervise.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "pino": "^9.5.0",
    "twilio": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vitest": "^2.1.5"
  }
}
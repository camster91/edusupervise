{
  "name": "@edusupervise/billing-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stripe billing adapter — mock + real implementations for EduSupervise.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "pino": "^9.5.0",
    "stripe": "~17.0.0"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vitest": "^3.2.7"
  }
}
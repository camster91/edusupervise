{
  "name": "@edusupervise/push",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mobile push notification helper — Expo HTTP API client. Shared between web and worker so both can dispatch reminders and notifications to mobile devices.",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@edusupervise/db": "workspace:*",
    "drizzle-orm": "~0.45.2",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.6.3",
    "vite-tsconfig-paths": "^5.1.0",
    "vitest": "^3.2.7"
  }
}
